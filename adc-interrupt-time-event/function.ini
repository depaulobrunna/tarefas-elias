FUNC void displaydata(void)
{
	uint32_t index;
	float volts;
	
	exec("log > Dados.log");
	
	for(index = 0; index < 4096; index++)
	{
		volts = (float)((float)(data[index])/(float)4095 * 3.0);
		printf("%02f\n",volts);
	}
		
	exec("log off");
}
